.filter {
  display: flex;
  margin: 32px 0;
  justify-content: space-between;
  @media screen and (max-width: 480px) {
    display: block;
  }
  &__category {
    &-name {
      text-transform: uppercase;
      margin-bottom: 12px;
      color: $myGrey;
    }
  }
}